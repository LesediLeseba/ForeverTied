# MemorialCode V1 — backend runtime dependencies (Python 3.11+)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.1.0
greenlet>=3.0.0
python-dotenv>=1.0.0

# --- local development / testing ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
# Portable PostgreSQL binaries so the stack can run without Docker in sandboxes.
# Optional: the app talks to any PostgreSQL 13+ instance over asyncpg.
pgserver>=0.1.4
